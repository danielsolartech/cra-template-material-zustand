import MainLayout from '@features/common/MainLayout';
import { IndexContainer } from '@features/index';

const Index: React.FC = () => (
	<MainLayout>
		<IndexContainer />
	</MainLayout>
);

export default Index;
